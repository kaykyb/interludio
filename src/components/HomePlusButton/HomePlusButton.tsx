import React, { Component } from "react";

import "./HomePlusButton.css";

export default class HomePlusButton extends Component {
  render() {
    return (
      <div className="HomePlusButton">
        <button className="load-more">ver mais</button>
      </div>
    );
  }
}
